{
  "name": "my-node-app",
  "version": "1.0.0",
  "description": "A simple Node.js web application",
  "main": "index.js",
  "scripts": {
    "test": "jest --verbose",
    "build": "echo 'Building the app...' && mkdir -p dist && cp index.js dist/ && echo 'Build complete.'"
  },
  "dependencies": {},
  "devDependencies": {
    "jest": "^29.0.0"
  },
  "jest": {
    "testEnvironment": "node"
  }
}